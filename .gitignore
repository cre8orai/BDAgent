# Raw mail and the derived corpus never leave this machine.
# Only voice/STYLE.md — the extracted, non-identifying style guide — is committed.
voice/raw/
voice/corpus/
voice/*.json

# LinkedIn first-degree graph — 7,246 real people. Private, like the corpus.
agents/state/connections.csv

# Machine-local permission grants. Interactive only, never used by a script.
.claude/settings.local.json

# Run locks and scratch
agents/state/.claim-*
agents/state/.*.py
agents/logs/*.log
agents/logs/launchd-*

.DS_Store
__pycache__/
